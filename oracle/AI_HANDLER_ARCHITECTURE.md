# 🔮 ORACLE AI Handler - Architecture & Workflow

## 🏗️ Architecture Globale

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ORACLE AI Handler Pipeline                       │
└─────────────────────────────────────────────────────────────────────┘

                         TELEGRAM WEBHOOK
                              │
                              ▼
                    ┌──────────────────┐
                    │ POST /webhook/   │
                    │   telegram       │
                    └──────────────────┘
                              │
                 ┌────────────┼────────────┐
                 ▼            ▼            ▼
            ┌─────────┐  ┌─────────┐  ┌──────────┐
            │ Extract │  │ Find/   │  │  Save   │
            │ Message │  │ Create  │  │ to DB   │
            │         │  │  User   │  │(MESSAGE)│
            └────┬────┘  └────┬────┘  └────┬────┘
                 │            │            │
                 └────────────┼────────────┘
                              │
                              ▼
                  ┌───────────────────────┐
                  │   AI HANDLER SERVICE  │
                  │  (core/ai_handler.py) │
                  └───────┬───────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  Get         │  │   Claude     │  │  Save        │
│  Unprocessed │  │   API Call   │  │  Response    │
│  Messages    │  │  (Haiku/     │  │  + Log       │
│  from DB     │  │   Sonnet)    │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │                │
        └─────────────────┼────────────────┘
                          │
                          ▼
                ┌──────────────────────┐
                │   RESPONSE SAVED     │
                │   - Message Table    │
                │   - SystemLog Table  │
                └──────────────────────┘
```

---

## 📊 Message Processing Flow

```
┌──────────────────────┐
│  New Telegram Msg    │
└──────────────┬───────┘
               │
               ▼
         ┌─────────────┐
         │ Webhook     │
         │ Receives    │
         └──────┬──────┘
                │
    ┌───────────┴───────────┐
    │                       │
    ▼                       ▼
┌─────────────┐      ┌──────────────┐
│  Create/    │      │  Create      │
│  Update     │      │  Message     │
│  User       │      │  Record      │
│  in Users   │      │  (PENDING)   │
│  Table      │      │              │
└─────────────┘      └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ Call         │
                    │ process_msg  │
                    │ _batch()     │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌──────────────┐   ┌──────────────┐  ┌──────────────┐
│ Get all      │   │ Classify msg │  │ Call Claude  │
│ unprocessed  │   │ for model:   │  │ API with     │
│ messages     │   │              │  │ selected     │
│              │   │ - Length?    │  │ model        │
│              │   │ - Keywords?  │  │              │
└──────────────┘   └──────┬───────┘  └──────┬───────┘
        │                 │                 │
        └─────────────────┼─────────────────┘
                          │
                          ▼
                  ┌───────────────┐
                  │  Save Result  │
                  │  - Response   │
                  │  - Tokens     │
                  │  - Model Used │
                  │  - Cost       │
                  └───────────────┘
```

---

## 🔄 Data Flow Diagram

```
                    ┌─ TELEGRAM ──┐
                    │   WEBHOOK   │
                    └──────┬──────┘
                           │
         USER MESSAGE: "Analyze crypto..."
                           │
                           ▼
                ┌──────────────────────┐
                │   Database: Users    │
                │ ┌──────────────────┐ │
                │ │ ID: 123          │ │
                │ │ Username: john   │ │
                │ │ Telegram_ID: ... │ │
                │ └──────────────────┘ │
                └──────────────────────┘
                           │
         MESSAGE RECORD:   │
         "Analyze crypto..."
                           │
                           ▼
                ┌──────────────────────┐
                │  Database: Messages  │
                │ ┌──────────────────┐ │
                │ │ ID: 42           │ │
                │ │ User_ID: 123     │ │
                │ │ Content: "A..."  │ │
                │ │ Type: "user_msg" │ │
                │ │ Model: NULL      │ │
                │ └──────────────────┘ │
                └──────────────────────┘
                           │
                           ▼
                   ┌─────────────────┐
                   │  AI HANDLER     │
                   │  - Detect msg   │
                   │  - Select model │
                   │  - Call Claude  │
                   └────────┬────────┘
                            │
         CLAUDE RESPONSE:   │
         "Based on analysis..."
                            │
                            ▼
                ┌──────────────────────┐
                │ Database: Messages   │
                │ ┌──────────────────┐ │
                │ │ ID: 43           │ │
                │ │ User_ID: 123     │ │
                │ │ Content: "B..."  │ │
                │ │ Type: "ai_resp"  │ │
                │ │ Model: "sonnet"  │ │
                │ │ Tokens: 412      │ │
                │ └──────────────────┘ │
                └──────────────────────┘
                            │
         LOG RECORD:        │
                            ▼
                ┌──────────────────────┐
                │  Database: SystemLog │
                │ ┌──────────────────┐ │
                │ │ Level: "INFO"    │ │
                │ │ Component: "ai.."│ │
                │ │ Message: "Proc.."│ │
                │ │ Tokens: 412      │ │
                │ └──────────────────┘ │
                └──────────────────────┘
```

---

## 🔌 Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│                       FASTAPI SERVER                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  POST /webhook/telegram                                    │
│  ├─ Parse Telegram Update                                  │
│  ├─ Create/Update User → [Users Table]                     │
│  ├─ Create Message Record → [Messages Table]               │
│  └─ Return OK                                              │
│                                                             │
│  POST /api/process-messages?limit=10                       │
│  ├─ Call ai_handler.process_message_batch()                │
│  ├─ AIHandler:                                             │
│  │  ├─ get_unprocessed_messages() → Query [Messages]       │
│  │  ├─ For each message:                                   │
│  │  │  ├─ process_message_with_claude()                    │
│  │  │  │  └─ Call Anthropic API                            │
│  │  │  └─ save_ai_response() → [Messages, SystemLog]       │
│  │  └─ Return stats                                        │
│  └─ Return JSON with results                               │
│                                                             │
│  GET /api/ai-handler/stats                                 │
│  ├─ Query [Messages] for statistics                        │
│  ├─ Count by type, model, status                           │
│  └─ Return JSON with stats                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
         │                 │              │
         ▼                 ▼              ▼
    ┌─────────┐      ┌──────────┐   ┌──────────┐
    │PostgreSQL│      │Anthropic │   │  Redis   │
    │Database  │      │Claude API│   │(Future)  │
    │          │      │          │   │          │
    │- Users   │      │- Haiku   │   │- Cache   │
    │- Messages│      │- Sonnet  │   │- Queue   │
    │- Logs    │      │- Opus    │   │          │
    │- Cache   │      │          │   │          │
    └─────────┘      └──────────┘   └──────────┘
```

---

## 🎯 Decision Tree - Model Selection

```
┌──────────────────────┐
│   New Message        │
│ Get Content Length   │
└──────────┬───────────┘
           │
           ▼
    ┌──────────────┐
    │ Length < 500 │
    │ characters?  │
    └──┬───────┬───┘
     Y │       │ N
       │       │
       ▼       ▼
    ┌──────┐  ┌─────────────────┐
    │HAIKU │  │ Check Keywords: │
    │      │  │ - "analyze"     │
    │€$$$$ │  │ - "strategy"    │
    │      │  │ - "complex"     │
    └──────┘  │ - "research"    │
              └──┬─────────┬────┘
              Y  │         │ N
                 ▼         ▼
              ┌──────┐   ┌──────┐
              │SONNET│   │HAIKU │
              │      │   │      │
              │€€€€€ │   │€$$$$ │
              └──────┘   └──────┘

Legend:
  $ = Cost (cheaper)
  €€ = Better quality
  ⚡ = Faster
```

---

## 📈 Data Model

### Tables Schema

```
USERS
├── id (PK)
├── telegram_id (UNIQUE)
├── username
├── first_name
├── last_name
├── is_premium (default: false)
├── created_at (index)
└── updated_at

MESSAGES
├── id (PK)
├── telegram_user_id (FK, INDEX)
├── message_id
├── content (TEXT)
├── message_type (user_msg|ai_response|system)
├── model_used (nullable: haiku|sonnet|opus)
├── tokens_used (default: 0)
└── created_at (INDEX)

SYSTEM_LOGS
├── id (PK)
├── level (INFO|WARNING|ERROR|DEBUG)
├── component (ai_handler|telegram_bot|...)
├── message (TEXT)
├── meta (JSON metadata)
└── created_at (INDEX)
```

---

## 🔒 Error Handling Strategy

```
┌─────────────────────┐
│   Claude API Call   │
└──────────┬──────────┘
           │
    ┌──────┴──────┐
    ▼             ▼
┌────────┐   ┌─────────────────┐
│ Success│   │ Error           │
└────────┘   └────────┬────────┘
                      │
           ┌──────────┼──────────┐
           │          │          │
           ▼          ▼          ▼
        ┌────┐   ┌────────┐  ┌─────────┐
        │401 │   │429/500 │  │Network  │
        │Auth│   │Timeout │  │Error    │
        └────┘   └────────┘  └─────────┘
           │          │          │
           ▼          ▼          ▼
      Log Error   Retry x3    Log Error
      Mark Failed  Backoff    Mark Failed
      Return to DB Return 0   Return 0
```

---

## 🚀 Deployment Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    PRODUCTION ENV                          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ FastAPI Server (Uvicorn/Gunicorn)                    │ │
│  │ ├─ Port 8000                                         │ │
│  │ ├─ Workers: 4                                        │ │
│  │ └─ Webhook endpoints active                          │ │
│  └──────────────────────────────────────────────────────┘ │
│           ▲              │              ▼                 │
│    ┌──────┴──────┐       │       ┌──────────────┐         │
│    │  Telegram   │       │       │ Claude API   │         │
│    │  Webhook    │       │       │ (cloud)      │         │
│    └─────────────┘       │       └──────────────┘         │
│                          │                                 │
│                  ┌───────▼────────┐                       │
│                  │  AI Handler    │                       │
│                  │  (Async Tasks) │                       │
│                  └───────┬────────┘                       │
│                          │                                 │
│    ┌─────────────────────┴──────────────────────┐         │
│    ▼                                             ▼         │
│ ┌────────────────┐                   ┌──────────────┐   │
│ │ PostgreSQL DB  │                   │ Redis Cache  │   │
│ │ (Persistent)   │                   │ (Session)    │   │
│ └────────────────┘                   └──────────────┘   │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 📊 Cost Estimation Example

```
┌────────────────────────────────────────────┐
│     Sample Message Processing Costs        │
└────────────────────────────────────────────┘

Message 1: "What's the best crypto strategy?"
├─ Length: 42 chars
├─ Keywords: "strategy" ✓
├─ Selected Model: SONNET (complex)
├─ Input tokens: ~150
├─ Output tokens: ~300
├─ Total tokens: ~450
└─ Cost: €0.00405

Message 2: "Analyze Bitcoin signals"
├─ Length: 23 chars
├─ Keywords: "analyze" ✓
├─ Selected Model: SONNET
├─ Input tokens: ~100
├─ Output tokens: ~280
├─ Total tokens: ~380
└─ Cost: €0.00342

Message 3: "Hello, how are you?"
├─ Length: 20 chars
├─ Keywords: none
├─ Selected Model: HAIKU (simple)
├─ Input tokens: ~50
├─ Output tokens: ~80
├─ Total tokens: ~130
└─ Cost: €0.00013

┌──────────────────────────────────────────┐
│  BATCH TOTAL                             │
├──────────────────────────────────────────┤
│ Messages: 3                              │
│ Total Tokens: 960                        │
│ Estimated Cost: €0.00760                 │
│ Cost per message: €0.00253               │
└──────────────────────────────────────────┘
```

---

## 🎯 Performance Characteristics

```
┌──────────────────────────────────────────────┐
│         Latency Breakdown                    │
├──────────────────────────────────────────────┤
│                                              │
│ Database Query:           ~5ms               │
│ Model Selection Logic:    ~1ms               │
│ Claude API Call:    1500-3000ms ⏱️            │
│ Response Processing:      ~10ms              │
│ Database Save:           ~20ms               │
│ Logging:                  ~5ms               │
│                          ──────              │
│ TOTAL per message:  1540-3040ms             │
│                                              │
│ Batch (10 msgs):   15-30 seconds            │
│ Throughput:        ~20-30 msg/min           │
│                                              │
└──────────────────────────────────────────────┘

Bottleneck: Claude API latency
Optimization: Background tasks + queueing
```

---

## 🔍 Monitoring Points

```
┌────────────────────────────────────────────┐
│      Monitoring & Alerts                   │
├────────────────────────────────────────────┤
│                                            │
│ ⚠️  Alert on:                              │
│   • Error rate > 5%                        │
│   • Processing latency > 5s                │
│   • API calls > 1000/day                   │
│   • Cost > €50/day                         │
│   • Database lag > 1000 rows                │
│                                            │
│ 📊 Metrics to track:                       │
│   • Messages processed/hour                │
│   • Average response time                  │
│   • Tokens used/day                        │
│   • Cost trend                             │
│   • Model distribution                     │
│   • Error rate by type                     │
│   • Queue depth                            │
│                                            │
└────────────────────────────────────────────┘
```

---

## 🚦 State Diagram

```
        ┌──────────────────┐
        │   NEW MESSAGE    │
        │  From Telegram   │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │    RECEIVED      │
        │ Webhook captures │
        │ User & Message   │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │     PENDING      │
        │ In DB, awaiting  │
        │ AI processing    │
        └────────┬─────────┘
                 │
           ┌─────┴──────┐
           │            │
           ▼            ▼
    ┌──────────────┐ ┌──────────────┐
    │  PROCESSING  │ │    ERROR     │
    │ Claude API   │ │ Failed call  │
    │ call ongoing │ │ Retry later  │
    └──────┬───────┘ └──────┬───────┘
           │                │
           ▼                ▼
    ┌──────────────┐ ┌──────────────┐
    │  COMPLETED   │ │  FAILED      │
    │ Response     │ │ Manual retry │
    │ saved to DB  │ │ needed       │
    └──────┬───────┘ └──────────────┘
           │
           ▼
    ┌──────────────────┐
    │   READY TO SEND  │
    │ Via Telegram Bot │
    │ to user          │
    └──────────────────┘
```

---

## 📞 Integration Points

```
┌─────────────────────────────────────────────────────────────┐
│              ORACLE AI Handler API Contracts                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 1. INPUT: Telegram Webhook                                 │
│    ├─ POST /webhook/telegram                               │
│    └─ Format: Telegram Update JSON                         │
│                                                             │
│ 2. OUTPUT: Message Processing                              │
│    ├─ GET /api/process-messages                            │
│    ├─ Returns: Processing summary + details                │
│    └─ Format: JSON with stats                              │
│                                                             │
│ 3. QUERY: AI Handler Stats                                 │
│    ├─ GET /api/ai-handler/stats                            │
│    └─ Returns: Message counts + model usage                │
│                                                             │
│ 4. DATA ACCESS: Messages API                               │
│    ├─ GET /api/messages?user_id=X&limit=100               │
│    └─ Returns: List of all messages                        │
│                                                             │
│ 5. MONITORING: Logs API                                    │
│    ├─ GET /api/logs?level=ERROR&limit=50                  │
│    └─ Returns: System logs for debugging                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎓 Extension Points

```
┌───────────────────────────────────┐
│  ORACLE AI Handler v1.0 - BASE    │
├───────────────────────────────────┤
│                                   │
│  Ready to extend with:            │
│                                   │
│  📧 Email Handler                 │
│     └─ Similar pattern to MsgH.   │
│                                   │
│  🐦 Twitter Handler               │
│     └─ Retrieve + Process tweets  │
│                                   │
│  💼 Notion Handler                │
│     └─ Sync to Notion DB          │
│                                   │
│  📊 Analytics Handler             │
│     └─ Track airdrop opportunities│
│                                   │
│  🔄 Scheduler Handler             │
│     └─ Periodic batch jobs        │
│                                   │
│  🛑 Rate Limiter                  │
│     └─ Per-user quotas            │
│                                   │
│  💾 Caching Layer                 │
│     └─ Redis cache integration    │
│                                   │
└───────────────────────────────────┘
```

---

*Last Updated: February 2, 2026*  
*Architecture Version: 1.0*  
*Status: Production Ready ✨*
